services:
  postgres:
    image: postgres:17
    container_name: filesharing-postgres
    environment:
      POSTGRES_DB: filesharingdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: souha
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: